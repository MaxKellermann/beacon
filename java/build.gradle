apply plugin: 'java-library'

/* This stops Gradle from forcing its idea of a directory structure on
   us. */
sourceSets {
    main {
        java {
            srcDirs = ['.']
        }
    }
}

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}
